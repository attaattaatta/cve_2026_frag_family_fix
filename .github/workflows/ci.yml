name: CI

on:
  push:
  pull_request:

jobs:
  alpine-build:
    name: Alpine Build
    runs-on: ubuntu-latest

    container:
      image: golang:1.26-alpine

    env:
      GOMODCACHE: /go/pkg/mod
      GOCACHE: /root/.cache/go-build

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Install deps
        run: apk add --no-cache git build-base ca-certificates

      - name: Cache Go modules
        uses: actions/cache@v4
        with:
          path: |
            /go/pkg/mod
            /root/.cache/go-build
          key: ${{ runner.os }}-go-${{ hashFiles('**/go.sum') }}

      - name: Build
        run: go mod download && go build -ldflags="-s -w" -o cve_2026_frag_family_fix cve_2026_frag_family_fix.go